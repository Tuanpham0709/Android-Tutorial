<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".model.Utilities"></activity>
        <activity android:name=".model.EditInfo">

        </activity>
        <activity android:name=".Controller.news_item_layout">

        </activity>
        <activity android:name=".Controller.Main.HomeActivity2">

        </activity>
        <activity android:name=".Controller.Auth.SplashActivity">

        </activity>
        <activity android:name=".Controller.Main.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Controller.Auth.LoginActivity" />
        <activity android:name=".Controller.Main.MainActivity" />
    </application>

</manifest>